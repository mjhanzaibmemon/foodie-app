name: Stage Deployment

on:
  push:
    branches: [ "stage" ]
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: SSH Deploy
      run: echo "Example stage deployment"
